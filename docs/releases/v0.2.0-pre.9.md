# XArchive v0.2.0-pre.9（已作废）

> 本版本的发布流程存在缺陷，未产生任何发布资产；发布内容由 [`v0.2.0-pre.10.md`](v0.2.0-pre.10.md) 取代。本文件仅作为 tag `v0.2.0-pre.9` 的历史记录保留。

## 作废事实

- tag `v0.2.0-pre.9` 指向 `4578bb8`（`release: prepare v0.2.0-pre.9 with driver banner fix and toolchain rule`），该 tag 保留，不移动；
- Linux Pre-Release run `35694515542` 成功创建了 GitHub pre-release（正文为本版本原发布说明，即现行 `v0.2.0-pre.10.md` 的前身）；
- Windows Release Build run `35695698631` 在步骤 1–16 全部通过（包括新的 WebDriver toolchain major 兼容性预检），但在步骤 17「Run final executable UI readiness gate」于执行 WDIO 之前即失败：

  ```text
  New-Item: An item with the specified name
  D:\a\_temp\xarchive-release-readiness already exists.
  ```

  根因是 gate 步骤脚本中重复的 `New-Item`，而诊断目录已被步骤 16 preflight 创建。WDIO E2E gate 未实际执行；
- 该 workflow 缺陷使该 tag 的 Windows 构建不可能成功，因此不会向该 tag 上传任何资产，也不得以本地构建或历史资产替代；
- GitHub Release `XArchive v0.2.0-pre.9` 已转为 draft 并标注作废；
- 正式预发布以 `v0.2.0-pre.10` 为准，其发布说明与原 pre.9 计划一致，并额外包含对上述 `New-Item` 缺陷的幂等修复。