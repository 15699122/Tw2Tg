# Optional local development configuration for the Tauri Desktop Sidecar.
# XARCHIVE_SIDECAR_ARGS must be a JSON string array so paths containing spaces
# and non-ASCII characters remain a single argument.
XARCHIVE_SIDECAR_PROGRAM=python3
XARCHIVE_SIDECAR_ARGS=["-m","xarchive_downloader"]